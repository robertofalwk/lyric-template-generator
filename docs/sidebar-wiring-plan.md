# Sidebar Wiring Plan - Lyric Lab V7

This document outlines the strategy for connecting the Studio Sidebar to the real backend services and repositories, moving away from stubs to a fully operational production environment.

## Phase 1: Assets Management
- **Status:** List works, everything else is mocked.
- **Goal:** Enable real uploads and project associations.
- **Actions:**
    - [ ] Implement `handleAssetUpload` using `/api/assets/upload`.
    - [ ] Add `handleAssetDelete` calling `DELETE /api/assets/[id]`.
    - [ ] Add `handleAssetSelect` to update project's `selectedBackgroundAssetId` via `PATCH /api/projects/[id]`.
    - [ ] Refactor UI to show upload progress.

## Phase 2: Template Library
- **Status:** List/Select works, CRUD and Favorites are mocked.
- **Goal:** Full lifecycle for custom and stock templates.
- **Actions:**
    - [ ] Implement `handleToggleFavorite(templateId)`.
    - [ ] Implement `handleDuplicateTemplate(templateId)`.
    - [ ] Implement `handleDeleteTemplate(templateId)`.
    - [ ] Ensure metadata (sourceType, version, score) is real.

## Phase 3: Studio Review (Quality Gates)
- **Status:** 100% Mocked.
- **Goal:** Connect to `TemplateQualityScorer` and `ProjectCommentRepository`.
- **Actions:**
    - [ ] Fetch real visual score and warnings from `/api/templates/[id]/score`.
    - [ ] Fetch/Post comments via `/api/projects/[id]/comments`.
    - [ ] Implement `handleApproveStrategy` updating project status to `approved`.

## Phase 4: Publish & History
- **Status:** 100% Mocked.
- **Goal:** Real render jobs history and downloads.
- **Actions:**
    - [ ] Fetch history from `/api/projects/[id]/jobs?type=render`.
    - [ ] Show real status (queued, processing, completed).
    - [ ] Enable valid download links to `/exports/...`.

## Phase 5: Monitor & Project Context
- **Status:** Creation works, persistence is good.
- **Goal:** Better re-hydration and project updates.
- **Actions:**
    - [ ] Allow editing title/lyrics for the active project.
    - [ ] Reflect active project state (Selected Template/Asset) across all tabs.

---
*Generated by Antigravity Studio Engineer v7*
